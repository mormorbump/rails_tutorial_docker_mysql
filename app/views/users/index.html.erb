<% provide(:title, 'All users') %>
<h1>All users</h1>

<!-- 挟むとページネーション。usersビューの中から間の@usersを探す。それがpaginateのインスタンスだったらページネーション。-->
<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>